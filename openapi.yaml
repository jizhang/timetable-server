# DO NOT EDIT
# Auto generated by bin\flask spec

info:
  description: What have you done today?
  title: Timetable
  version: 0.1.0
servers:
- url: http://localhost:1234
paths:
  /note/save:
    post:
      summary: Save note.
      tags:
      - note
      x-swagger-router-controller: timetable.views.note
      operationId: save_note
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/NoteForm'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
openapi: 3.0.2
components:
  schemas:
    NoteForm:
      type: object
      properties:
        content:
          type: string
    Note:
      type: object
      properties:
        created:
          type: string
          format: date-time
        content:
          type: string
